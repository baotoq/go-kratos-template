# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Coffee API
    description: 'Coffee is a tiny Dapr Workflow demo: brew a cup, then check on it.'
    version: 0.0.1
paths:
    /v1/coffee:
        post:
            tags:
                - Coffee
            description: Start brewing a cup. Returns an instance_id you can poll.
            operationId: Coffee_Brew
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/coffee.v1.BrewRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/coffee.v1.BrewReply'
    /v1/coffee/{instanceId}:
        get:
            tags:
                - Coffee
            description: Check on a brewing cup by instance_id.
            operationId: Coffee_Check
            parameters:
                - name: instanceId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/coffee.v1.CheckReply'
components:
    schemas:
        coffee.v1.BrewReply:
            type: object
            properties:
                instanceId:
                    type: string
        coffee.v1.BrewRequest:
            type: object
            properties:
                beans:
                    type: string
                size:
                    type: string
        coffee.v1.CheckReply:
            type: object
            properties:
                instanceId:
                    type: string
                status:
                    type: string
                    description: RUNNING, COMPLETED, FAILED, etc.
                cup:
                    type: string
                    description: Final coffee description once status == COMPLETED.
tags:
    - name: Coffee
